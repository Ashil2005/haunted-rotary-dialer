<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent">

    <!-- Subtle transparent background covering keypad area (bottom 60%) -->
    <View
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="bottom"
        android:layout_marginTop="300dp"
        android:background="#40000000" />

    <!-- Rotary Dial View - centered in bottom area -->
    <com.example.rotary_dialer.RotaryDialView
        android:id="@+id/rotary_dial_view"
        android:layout_width="match_parent"
        android:layout_height="400dp"
        android:layout_gravity="bottom"
        android:layout_marginBottom="120dp"
        android:layout_marginStart="32dp"
        android:layout_marginEnd="32dp" />

    <!-- Control panel below dial -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:layout_marginBottom="20dp"
        android:layout_marginStart="32dp"
        android:layout_marginEnd="32dp"
        android:orientation="vertical"
        android:gravity="center">

        <!-- Number display -->
        <TextView
            android:id="@+id/number_display"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/number_display_bg"
            android:padding="16dp"
            android:text=""
            android:textColor="#FFC857"
            android:textSize="24sp"
            android:fontFamily="monospace"
            android:gravity="center"
            android:hint="Dial a number"
            android:textColorHint="#A44A16"
            android:elevation="4dp" />

        <!-- Button row: Delete and Call -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:orientation="horizontal"
            android:gravity="center"
            android:weightSum="2">

            <!-- Delete button - Dark cracked stone theme -->
            <Button
                android:id="@+id/delete_button"
                android:layout_width="0dp"
                android:layout_height="56dp"
                android:layout_weight="1"
                android:layout_marginEnd="8dp"
                android:background="@drawable/delete_button_bg"
                android:text="⌫ DELETE"
                android:textColor="#E0E0E0"
                android:textSize="14sp"
                android:fontFamily="sans-serif-medium"
                android:elevation="6dp" />

            <!-- Call button - Toxic green theme -->
            <Button
                android:id="@+id/call_button"
                android:layout_width="0dp"
                android:layout_height="56dp"
                android:layout_weight="1"
                android:layout_marginStart="8dp"
                android:background="@drawable/call_button_bg"
                android:text="📞 CALL"
                android:textColor="#FFFFFF"
                android:textSize="14sp"
                android:fontFamily="sans-serif-medium"
                android:elevation="6dp" />

        </LinearLayout>

    </LinearLayout>

    <!-- Close button at top-right -->
    <ImageButton
        android:id="@+id/close_button"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_gravity="top|end"
        android:layout_marginTop="40dp"
        android:layout_marginEnd="16dp"
        android:background="#80000000"
        android:contentDescription="Close overlay"
        android:src="@android:drawable/ic_menu_close_clear_cancel"
        android:padding="12dp"
        android:scaleType="fitCenter"
        android:tint="#FFFFFF" />

</FrameLayout>
